# If you come from bash you might have to change your $PATH.
export GOPATH=$HOME/go
export PATH=$GOPATH/bin:$HOME/bin:$HOME/.local/bin:/usr/local/opt/curl/bin:/usr/local/bin:/usr/local/go/bin:$PATH
export EDITOR="nvim"

# Created by Zap installer
[ -f "${XDG_DATA_HOME:-$HOME/.local/share}/zap/zap.zsh" ] && source "${XDG_DATA_HOME:-$HOME/.local/share}/zap/zap.zsh"
plug "zsh-users/zsh-autosuggestions"
plug "zap-zsh/supercharge"
plug "zap-zsh/zap-prompt"
plug "zsh-users/zsh-syntax-highlighting"
plug "zap-zsh/fzf"
plug "$HOME/.oh-my-zsh/plugins/git/git.plugin.zsh"
plug "$HOME/.oh-my-zsh/plugins/tmux/tmux.plugin.zsh"

# Load and initialise completion system
autoload -Uz compinit && compinit
# complete -o nospace -C $HOME/bin/terraform terraform

alias ws="cd ~/workspace"
alias vim="nvim"
alias cat="bat"
alias ls="eza"

eval "$(starship init zsh)"
